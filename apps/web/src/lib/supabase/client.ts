/**
 * Browser Supabase client. Prefer `@xuanvu/shared/supabase/browser` for shared behavior;
 * this path exists so `apps/web` matches the standard `src/lib/supabase` layout.
 */
export { createSupabaseBrowserClient } from "@xuanvu/shared/supabase/browser";
