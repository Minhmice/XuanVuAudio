"use client";

/**
 * Same as AppButton — use `isLoading` for spinner. Exported as a named alias for discoverability.
 */
export { AppButton as LoadingButton, type AppButtonProps as LoadingButtonProps } from "./app-button";
